#include"shell.h"

int main (void)
{
    char str[] = "deadass";
    char *start = remove_spaces(str);
    printf("the trimed string is: %s\n", start);
    free(start);
    start = NULL;
    return(0);
}