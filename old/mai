#include"shell.h"

int main (void)
{
    char str[] = "       deadass      gffg";
    char *start = remove_spaces(str);
    printf("the trimed string is:%s", start);
    free(start);
    start = NULL;
    return(0);
}